const Strings = {};

const InitialState = {
  isDrawerOpen: false,
  isTooltipVisible: false,
  portfolios: undefined,
  userId: null,
  newPortfolio: { name: "" },
};

const Setup = {
  Strings,
  InitialState,
};

export default Setup;
